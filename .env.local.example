# Base URL of the FastAPI backend (no trailing slash).
# The WebSocket URL is derived automatically by replacing http -> ws.
# In production, use your deployed backend URL (https://... -> wss://... is handled automatically).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
